{erl_opts, [debug_info]}.
{deps, []}.

{shell, [
    %% ако имаш нужда от sys.config:
    %% {config, "config/sys.config"},
    {apps, [
        main_app,
        common_lib,
        docker_app,
        kafka,
        chrome,
        raptors
    ]}
]}.